<h1 align="center">Welcome back <%= @user.username%>'s to page</h1>
<div class="row">
	<div class="col col-md offset-md-5">
		<%= gravatar_for @user, size: 150%>
	</div>
</div>
<h4 align="center">Articles <%= @user.username%></h4>

<%= render 'articles/article', obj: @user.articles%>